[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "yolop-memory"
version = "0.1.0"
description = "Scoped persistent memory for YoloP"
requires-python = ">=3.12"
dependencies = [
    "pydantic-ai-slim>=2.31.0",
    "yolop-runtime>=0.1.0,<0.2.0",
]

[tool.hatch.build.targets.wheel]
packages = ["yolop_memory"]
